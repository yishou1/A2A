# Conditional Tabular GAN Service

Run from the repository root:

```powershell
python services/conditional_tabular_gan/app/main.py
```

The service listens on `9035` by default and always labels generated output as
synthetic data.
