# Service Contract

- `GET /health` verifies implementation and reference-experiment artifacts.
- `GET /metadata` reports M12 and the FedAvg protocol identity.
- `POST /predict` performs one server-side aggregation round.

Default endpoint: `http://127.0.0.1:9034/predict`.
