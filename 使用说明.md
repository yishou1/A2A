# A2A 算法库与 AMOS 融合界面使用说明

## 1. 推荐使用方式

日常查看和演示时，使用根目录的融合启动器。它会统一启动：

- AlgoLib Server：`127.0.0.1:8088`
- AMOS：`127.0.0.1:5000`
- AMOS 同源托管的算法管理前端：`/algolib/`

不需要另外运行 Vite，也不需要访问 `5173`。只有进行算法管理前端源码开发时，才单独启动 Vite 开发服务器。

## 2. 首次使用前检查

在 PowerShell 中进入仓库根目录：

```powershell
cd D:\bupt\P1\A2A-lkf-algorithmrepo
```

当前机器应具备：

1. AMOS Python 环境：`amos-platform\.venv\Scripts\python.exe`。
2. AlgoLib 可执行文件：优先使用 `build-smoke-offline\Debug\algolib_server.exe`，也支持脚本中列出的其他构建位置。
3. 前端依赖：`algorithmrepo\web\node_modules`。

如果 AMOS 环境尚未安装：

```powershell
python -m venv .\amos-platform\.venv
.\amos-platform\.venv\Scripts\python.exe -m pip install -e ".\amos-platform"
```

如果算法管理前端依赖尚未安装：

```powershell
cd .\algorithmrepo\web
npm ci
cd ..\..
```

若缺少 AlgoLib 可执行文件，需要先配置 CMake 和 C++ 编译环境，再编译 `algorithmrepo/CMakeLists.txt`。当前开发机已经存在可供融合启动器使用的构建结果。

## 3. 日常启动与停止

### 3.1 最简单的启动方式

双击根目录：

```text
启动融合界面.bat
```

脚本完成后会自动打开 AMOS。也可以在 PowerShell 中执行：

```powershell
.\scripts\start_integrated_ui.ps1 -OpenBrowser
```

访问地址：

- AMOS：<http://127.0.0.1:5000/>
- 算法管理台：<http://127.0.0.1:5000/algolib/algorithms>
- AlgoLib 健康检查：<http://127.0.0.1:8088/health>

启动器会隐藏后台服务窗口，并在服务通过健康检查后返回。日志位于：

```text
.runtime\integrated-ui\logs\
```

### 3.2 停止方式

双击：

```text
停止融合界面.bat
```

或者执行：

```powershell
.\scripts\stop_integrated_ui.ps1
```

不要仅关闭浏览器页面来代替停止服务，浏览器关闭后后台进程仍可能继续运行。

### 3.3 快速检查

```powershell
Invoke-RestMethod http://127.0.0.1:8088/health
Invoke-WebRequest http://127.0.0.1:5000/ -UseBasicParsing
Invoke-WebRequest http://127.0.0.1:5000/algolib/algorithms -UseBasicParsing
```

算法管理台右上角显示“服务正常”即表示浏览器、AMOS 代理和 AlgoLib 的基本链路正常。

## 4. 为什么默认只看到 11 个算法

仓库中有 23 个已验证算法包：

- 11 个 ONNX 算法；
- 12 个 Python HTTP 算法。

融合启动器首次建立 Registry 时，只自动注册并激活 11 个已验证 ONNX 算法。Python HTTP 算法在注册验证时必须能够访问相应的 Python 服务，所以不会在服务未启动时自动注册。

Registry 保存在：

```text
algorithmrepo\.algolib\registry.json
```

算法的注册、激活和禁用状态会写入该文件，正常停止和重新启动后仍会保留。该文件属于本机运行状态，不提交到 Git；删除它或更换 Registry 路径会得到一套新的状态。

## 5. 使用 Python HTTP 算法

### 5.1 启动服务

在新的 PowerShell 窗口中执行：

```powershell
cd D:\bupt\P1\A2A-lkf-algorithmrepo
.\algorithmrepo\scripts\start_a2a_algorithm_services.ps1 `
  -Python (Get-Command python).Source `
  -LegacyOnly
```

`-LegacyOnly` 会启动 15 个传统算法服务，包含推荐演示使用的三个算法：

| 算法 | 端口 |
|---|---:|
| `execution_rule_matcher` | 9010 |
| `trajectory_linear_predictor` | 9011 |
| `threat_priority_random_forest` | 9032 |

任务栏出现多个最小化 Python 窗口属于当前脚本的正常行为。不要重复运行该脚本，否则可能出现端口占用。

检查服务：

```powershell
Invoke-RestMethod http://127.0.0.1:9010/health
Invoke-RestMethod http://127.0.0.1:9011/health
Invoke-RestMethod http://127.0.0.1:9032/health
```

### 5.2 在界面注册

打开“算法目录”，点击“注册算法”，依次填写 AlgoLib Server 可以访问的目录：

```text
examples/execution_rule_matcher/1.0.0
examples/trajectory_linear_predictor/1.0.0
examples/threat_priority_random_forest/1.0.0
```

AlgoLib Server 的工作目录是 `algorithmrepo/`，所以上述相对路径有效。也可以填写完整绝对路径。

注册成功后进入算法详情：

1. 确认状态为“已验证”。
2. 点击“激活”。
3. 返回“在线调用”，确认算法出现在选择列表中。

注册按钮是“服务器路径注册”，不是从浏览器上传算法包。

### 5.3 关闭 Python 算法服务

可以逐个关闭对应的 Python 窗口。需要一次停止本项目算法服务时，在 PowerShell 中执行：

```powershell
Get-CimInstance Win32_Process |
  Where-Object {
    $_.Name -eq 'python.exe' -and
    $_.CommandLine -like '*algorithmrepo\services\*'
  } |
  ForEach-Object { Stop-Process -Id $_.ProcessId }
```

该命令按命令行路径筛选，只针对 `algorithmrepo/services/` 下启动的 Python 服务。

## 6. 界面功能使用

### 6.1 运行总览

查看 AlgoLib 是否在线、算法状态分布、部署数量、Runner Cache、Registry 路径和执行日志路径。服务异常时先检查此页，再查看 `.runtime/integrated-ui/logs/`。

### 6.2 算法目录

可以按名称、算法 ID、状态和后端类型筛选。点击算法名称进入详情；需要新增算法时使用“注册算法”并填写服务器路径。

### 6.3 算法生命周期

典型顺序为：

```text
注册 → 验证 → 激活 → 在线调用 → 禁用
```

注意：

- “重新验证”只重新读取和校验算法卡、Schema，不等于启用算法。
- 已禁用算法重新验证后仍保持“已禁用”，需要点击“激活”才能恢复调用。
- 删除为逻辑删除，并要求输入算法 ID 二次确认。

### 6.4 在线调用

1. 选择已激活算法。
2. 选择算法卡示例或 Schema 缺省值模板，也可以手工填写。
3. 复杂数组和对象建议切换到“JSON 模式”。
4. 选择“业务功能点”；请求必须包含 `function_id` 或 `function_code`，否则后端会拒绝调用。
5. 填写 Workflow ID 和 Step ID；需要串联多次演示时保持同一 Trace ID。
6. 点击“执行调用”。
7. 成功后可查看 Outputs、Usage 和功能执行信息，并跳转“执行链路”。

每次独立调用应使用新的 Request ID。多次调用使用相同 Trace ID 和 Workflow ID、不同 Step ID，即可在“执行链路”中按顺序查看。

### 6.5 功能点矩阵与 KC

KC 是系统业务功能点的编号，例如：

- `KC-17`：目标/威胁优先级判定；
- `KC-19`：航迹维持；
- `KC-21`：交战规则校验。

功能点矩阵用于查看某个业务功能由哪些算法支持、算法承担主用还是辅助角色，以及覆盖程度。

## 7. 推荐演示流程

建议演示“威胁排序 → 轨迹预测 → 规则匹配 → ONNX 合规评分”。

统一设置：

```text
Trace ID: trace-demo-mission-001
Workflow ID: workflow-demo-mission-001
```

### 第一步：威胁优先级

- 算法：`threat_priority_random_forest / 1.0.0 / Python HTTP`
- 功能点：`KC-17 / prioritize_target_threat`
- Step ID：`step-01-prioritize-threat`

Inputs：

```json
{
  "targets": [
    {"target_id":"TGT-HIGH","threat_score":0.95,"distance_km":5.0,"speed_mps":420.0,"asset_value":0.9,"intel_confidence":0.95},
    {"target_id":"TGT-LOW","threat_score":0.12,"distance_km":140.0,"speed_mps":20.0,"asset_value":0.1,"intel_confidence":0.7},
    {"target_id":"TGT-MEDIUM","threat_score":0.55,"distance_km":65.0,"speed_mps":180.0,"asset_value":0.5,"intel_confidence":0.8}
  ]
}
```

Params：

```json
{"sort_descending":true}
```

### 第二步：轨迹预测

- 算法：`trajectory_linear_predictor / 1.0.0 / Python HTTP`
- 功能点：`KC-19 / maintain_track`
- Step ID：`step-02-predict-trajectory`

Inputs：

```json
{
  "track": {
    "track_id": "TGT-HIGH",
    "history": [
      {"t":0.0,"x":10.0,"y":18.0},
      {"t":0.1,"x":10.4,"y":18.6},
      {"t":0.2,"x":10.9,"y":19.1},
      {"t":0.3,"x":11.3,"y":19.7},
      {"t":0.4,"x":11.8,"y":20.2}
    ],
    "weapon_prep_sec": 2.0,
    "flight_time_sec": 4.0
  }
}
```

Params：`{}`。

### 第三步：执行规则匹配

- 算法：`execution_rule_matcher / 1.0.0 / Python HTTP`
- 功能点：`KC-21 / verify_rules_of_engagement`
- Step ID：`step-03-match-rule`

Inputs：

```json
{
  "phase": "strike",
  "situation": {
    "threat_score": 0.75,
    "intel_confidence": 0.82,
    "resource_readiness": 0.81,
    "communication_quality": 0.9
  }
}
```

Params：`{}`。

### 第四步：ONNX 合规评分

- 算法：`compliance_risk_scorer_onnx / 1.0.0 / ONNX`
- 功能点：`KC-21 / verify_rules_of_engagement`
- Step ID：`step-04-check-compliance`

Inputs：

```json
{"features":[[0.0,1.0,0.2,0.0,2.0,0.0]]}
```

Params：`{}`。

最后打开“执行链路”，输入 `trace-demo-mission-001`，展示四个步骤的算法、后端、功能点、状态和耗时。

## 8. 前端开发方式

只有修改算法管理前端源码时才执行：

```powershell
cd D:\bupt\P1\A2A-lkf-algorithmrepo\algorithmrepo\web
npm run dev -- --host 127.0.0.1
```

开发地址通常为 `http://127.0.0.1:5173/`，Vite 会把 `/api/*` 代理到 `127.0.0.1:8088`。如果端口被占用，以终端实际显示的地址为准。

常用验证命令：

```powershell
npm run test
npm run build
npm run test:e2e
npm run test:e2e:real
npm run test:e2e:integrated
```

## 9. 常见问题

### 9.1 无法打开 `/algorithms`

融合模式的正确地址是：

```text
http://127.0.0.1:5000/algolib/algorithms
```

`http://127.0.0.1:5173/algorithms` 只在 Vite 开发服务器已单独启动时有效。

### 9.2 页面显示服务异常

先检查 `http://127.0.0.1:8088/health`，再查看：

```text
.runtime\integrated-ui\logs\algolib.err.log
.runtime\integrated-ui\logs\amos.err.log
```

### 9.3 调用提示必须包含 `function_id` 或 `function_code`

在在线调用页面选择“业务功能点”。不要只填写 Inputs、Params、Workflow ID 和 Step ID。

### 9.4 Python HTTP 调用失败

确认对应端口的 `/health` 正常，并确认算法已经注册且激活。注册成功不代表服务永远在线；Python 进程关闭后调用仍会失败。

### 9.5 启动时提示端口占用

先运行：

```powershell
.\scripts\stop_integrated_ui.ps1
```

如果是 Python 算法端口占用，按 5.3 节停止旧算法服务后再启动。

### 9.6 `No module named amos_platform`

说明当前 Python 环境没有安装 AMOS。执行：

```powershell
.\amos-platform\.venv\Scripts\python.exe -m pip install -e ".\amos-platform"
```

如果安装依赖时手动取消，安装不会完成，需要重新执行并等待命令正常结束。

### 9.7 可以使用根目录的“启动项目.bat”吗

当前算法管理演示应使用“启动融合界面.bat”。“启动项目.bat”来自完整 A2A 链路，当前仍包含旧机器绝对路径，未完成可移植化前不作为本目录的推荐入口。

## 10. Git 提交注意事项

不要提交以下本地生成内容：

- `.algolib/`
- `build*/`
- `node_modules/`
- `dist/`
- `.runtime/`
- 测试结果和日志

提交前建议执行：

```powershell
git status --short
git diff --cached --stat
```

优先按文件或目录精确执行 `git add`，避免在未检查状态时直接提交整个工作区。

