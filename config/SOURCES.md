# Source provenance

This integration preserves the original repositories as copied source trees.

- `commander/`: `yishou1/A2A`, branch `integration/main-multi-agent`, commit
  `ddce95f185e2397f475c20bc3afac2c13d5c466b`.
- `amos-platform/`: `zzyua18/amos-platform`, branch `midterm-review`, commit
  `783275721fdb01f1fbce9e534429b458dd907bfd`.
- Missing algorithm-library files merged into `commander/`: `yishou1/A2A`,
  branch `cmax/algorithmrepo`, commit
  `75f9cdaf98292c3782d9312ca89af67a2a35a5bd`.

The algorithm import adds the C++ AlgoLib implementation, package manifests,
Python HTTP services, example ONNX models, and only helper files absent from the
Commander branch. Existing Commander Agent files were not overwritten.

