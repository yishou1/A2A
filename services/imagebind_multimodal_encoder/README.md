# ImageBind Multimodal Encoder Service

TIA algorithm packaged for the A2A algorithm library.

Run:

```powershell
$env:TIA_USE_MOCK="1"
$env:PORT="9025"
python services/imagebind_multimodal_encoder/app/main.py
```
