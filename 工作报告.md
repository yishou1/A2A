# A2A 算法库与 AMOS 前端融合工作报告

## 1. 报告概况

| 项目 | 内容 |
|---|---|
| 项目名称 | A2A 算法库管理平台与 AMOS 仿真平台融合 |
| 当前分支 | `integration/maritime-algolib` |
| 报告日期 | 2026-09-11 |
| 当前版本定位 | 可启动、可联调、可演示的阶段版本 |
| 权限范围 | 按当前要求，不建设登录、认证和角色权限模块 |

## 2. 总体完成情况

本阶段已将独立的算法库工程合入 A2A 项目，并完成 AMOS 与算法管理台的第一阶段融合。现在可以从 AMOS 页面进入算法管理台，也可以从算法管理台返回 AMOS；生产构建后的算法管理台由 AMOS 在同一 `5000` 端口提供，不再要求演示人员单独维护一个 `5173` 前端端口。算法管理台也已切换到与 AMOS 一致的深色海图指挥台视觉体系。

算法管理端的主要业务闭环已经具备：算法查看与注册、生命周期管理、部署管理、Schema/JSON 在线调用、功能点矩阵、执行链路查询，以及常见调用失败的检测、分类、提示和重试。当前版本适合项目展示和继续开展系统联调，但距离正式生产交付仍有自动化注册、全链路统一启动、部署硬化和更完整验收等工作。

## 3. 本阶段完成的主要工作

### 3.1 代码与工程整合

- 将算法库工程作为 `algorithmrepo/` 纳入 A2A 单仓库，保留算法卡、C++ 核心、Python HTTP 服务、管理前端、测试和说明文档。
- 在 `integration/maritime-algolib` 分支持续开发，保留原 AMOS、Commander 和 Agent 工程结构。
- 增加根目录构建与运行产物忽略规则，排除 `.algolib/`、`build*/` 和生成的根目录 `web/`，避免把大体积依赖、二进制和运行日志误提交到 Git。

### 3.2 两个前端的第一阶段融合

当前采用“统一入口、同源托管、工程暂时独立”的融合方案：

- AMOS 增加“算法与功能”入口，可跳转到 `/algolib/algorithms`。
- 算法管理台增加“AMOS 任务台”入口，可返回 AMOS 主界面。
- AMOS 后端从 `algorithmrepo/web/dist` 提供算法管理 React SPA。
- AMOS 增加 SPA 路由回退，直接刷新算法详情等深层地址不会出现 404。
- `/algolib-api/*` 由 AMOS 反向代理至 AlgoLib Server 的 `8088` 端口，浏览器侧不需要跨域访问。
- AMOS 中的算法记录支持跳转到算法管理台对应详情页，实现业务页面与管理页面的上下文衔接。
- 算法管理台统一采用 AMOS 的深蓝黑背景、青蓝强调色、细边框、小圆角、紧凑数据密度和等宽技术字段。
- 顶栏采用与 AMOS 相同的 `Simulation / 场景仿真平台` 品牌层级，并显示算法工作区、服务在线状态和 Runner 信息。
- 侧栏、指标卡、表格、表单、按钮、状态提示、代码结果区和滚动条均完成暗色主题适配。

该方案已经解决双前端入口割裂、端口记忆和跨域配置问题，同时保留两个前端工程的独立边界，便于后续逐步统一视觉和业务上下文。

### 3.3 统一启停能力

新增以下入口：

- `启动融合界面.bat`
- `停止融合界面.bat`
- `scripts/start_integrated_ui.ps1`
- `scripts/stop_integrated_ui.ps1`

启动脚本能够：

1. 检查 AMOS Python、AlgoLib Server 和前端依赖。
2. 构建算法管理前端生产资源。
3. 首次运行时建立独立的 `algorithmrepo/.algolib/registry.json`。
4. 自动纳管并激活已验证的 ONNX 算法。
5. 以隐藏窗口方式启动 AlgoLib `8088` 和 AMOS `5000`。
6. 等待健康检查通过后输出访问地址。
7. 将日志和 PID 信息写入 `.runtime/integrated-ui/`。

停止脚本会核对 PID 与进程启动时间，只停止由融合启动器创建的进程，降低误关其他 Python 或服务进程的风险。

### 3.4 算法管理功能

管理前端现有七个主要页面：

| 页面 | 已实现能力 |
|---|---|
| 运行总览 | 服务健康、算法状态、部署状态、Registry 和执行日志信息 |
| 算法目录 | 搜索、筛选、分页、服务器路径注册和算法详情跳转 |
| 算法详情 | 算法卡、Schema、性能资源、功能点、部署和生命周期操作 |
| 部署管理 | 部署登记、加载、卸载、状态更新和删除 |
| 在线调用 | Schema 表单、JSON 模式、请求模板、上下文和执行结果 |
| 功能点矩阵 | 28 个 KC 功能点与算法、角色及覆盖程度的关联展示 |
| 执行链路 | 按 Trace ID 查询算法执行事件及耗时、状态和上下文 |

生命周期已经支持注册、验证、激活、禁用和逻辑删除。针对“禁用后重新验证”的语义已完成验证：重新验证只校验算法卡和 Schema，不会擅自把禁用状态改回已验证；需要恢复服务时由用户显式执行“激活”。

### 3.5 输入缺省值与演示模板

- 根据 JSON Schema 的 `default` 和 `examples` 生成类型安全的缺省输入。
- 优先提取算法卡中的完整对象输入示例，形成可选择的请求模板。
- 加载模板时同步更新 Schema 表单和 JSON 模式，并清除上一轮结果。
- 保留当前 Request ID 和 Trace ID，便于演示人员主动组织同一条执行链路。
- 复杂输入无法方便映射为表单时，可以直接使用 JSON 模式。

因此系统已经具备缺省值设置，但缺省值来源取决于对应算法卡和 JSON Schema 是否提供 `default` 或 `examples`；未声明时不会由前端随意构造业务数据。

### 3.6 错误检测与容错机制

本阶段重点补充了在线调用的错误检测、上下文保留和恢复能力：

- 使用 AJV 在浏览器端按 JSON Schema 校验输入，非法数据在请求发出前被拦截。
- 保留后端返回的 `error_code`、错误消息、Request ID 和 Trace ID，不再把已知错误笼统显示为 `UNKNOWN_ERROR`。
- 将 HTTP `502/503` 归类为 `SERVICE_UNAVAILABLE`，将 `504` 归类为 `SERVICE_TIMEOUT`。
- 网络断开归类为 `NETWORK_ERROR`。
- 对网络错误、服务不可用和服务超时提供“重试本次请求”，同时保留原请求内容。
- 对输入校验等不可恢复错误不进行盲目自动重试。
- 所有变更请求默认不自动重试，防止注册、激活、禁用、加载等操作被重复执行。
- 服务健康检查失败时提示数据可能过期，并禁用关键变更入口。
- 生命周期和部署操作补充确认提示；删除算法需要输入算法 ID，ready 部署要求先卸载再删除。
- 对页面状态过期导致的 `409` 冲突保留后端真实信息，不用前端旧状态覆盖服务端判断。

### 3.7 自动化验证

当前已建立三层验证：

- Vitest：生命周期规则、展示格式、Schema 默认值、请求模板、AJV 校验和错误归一化。
- Mock Playwright E2E：页面导航、列表筛选，以及 `503`、`504`、网络断开与重试。
- 真实 Playwright E2E：真实 ONNX、真实 Python HTTP、执行链路、非法输入拦截，以及“禁用后重新验证仍保持禁用、随后重新激活”的生命周期语义。

AMOS 侧同时增加算法管理入口、静态资源托管、深层路由和 API 代理测试，并提供 `npm run test:e2e:integrated` 验证融合部署。

## 4. 当前算法资产情况

仓库 `algorithmrepo/examples/` 当前共有 43 份算法卡：

| 后端类型 | 已验证 | 草稿 | 合计 |
|---|---:|---:|---:|
| ONNX | 11 | 1 | 12 |
| Python HTTP | 12 | 19 | 31 |
| 合计 | 23 | 20 | 43 |

当前本机融合 Registry 中为 11 个已激活 ONNX 算法。原因是融合启动器为了保证首次启动稳定，只会自动纳管自包含且已验证的 ONNX 包。12 个已验证的 Python HTTP 算法具备实际服务实现，但注册验证依赖其 HTTP 服务在线，因此需要先启动对应服务，再从算法管理台注册和激活。

`start_a2a_algorithm_services.ps1` 当前定义了 26 个 Python HTTP 服务启动项，其中包含已验证和草稿算法；“存在服务代码”不等于“已经通过算法库验证并适合正式演示”。演示应优先选择状态为 `validated` 的算法卡。

## 5. 当前访问和运行结构

| 组件 | 地址 | 说明 |
|---|---|---|
| AMOS 任务台 | `http://127.0.0.1:5000/` | 主入口 |
| 算法管理台 | `http://127.0.0.1:5000/algolib/algorithms` | 由 AMOS 同源托管 |
| AlgoLib 健康检查 | `http://127.0.0.1:8088/health` | C++ 服务 |
| Python HTTP 服务 | `9010`—`9038` 中的已配置端口 | 按需启动 |

融合启动器只负责 AMOS、AlgoLib 和算法管理前端，不会启动 Commander、Gateway、Nacos、全部 Agent 或全部 Python 算法服务。

## 6. 当前结论与剩余工作

### 6.1 当前结论

算法管理前端的核心功能已经比较完善，双前端也已达到统一入口和同源部署的第一阶段目标，可以进行管理界面和代表性算法调用演示。当前并非最终生产版本，也不是把两个前端源码完全合并成一个前端工程。

### 6.2 下一阶段建议

1. 增加已验证 Python HTTP 算法的批量启动、健康检查、注册和激活工具，减少人工操作。
2. 将融合界面启动器与完整 A2A 链路启动器统一，覆盖 Commander、Gateway、Nacos 和 Agent。
3. 在现有统一视觉基础上，继续统一 AMOS 与算法管理台的任务上下文和跨页面操作语义。
4. 从 AMOS 当前任务或算法覆盖项直接进入带算法参数、Trace ID 和 Workflow ID 的管理详情或调用页面。
5. 补齐真实多算法业务链路、服务异常矩阵、长时间运行和资源占用验收。
6. 完善生产反向代理、配置外置、离线安装、日志轮转、可观测性和交付包。
7. 修正或替换继承自旧工程、包含固定绝对路径的完整项目批处理脚本，避免换机器后无法启动。

## 7. 相关提交

| 提交 | 内容 |
|---|---|
| `d7d37ef5` | 将算法库整合到 A2A 单仓库 |
| `e523064d` | 建立 AMOS 与算法管理台的双向入口 |
| `2d1bbdf8` | 由 AMOS 同源托管算法管理台并代理 API |
| `ced9d94e` | 增加融合启停脚本和算法详情深链 |
| `fd51c21c` | 忽略本地 Registry、构建产物和生成前端目录 |
