# Local Knowledge Base

This directory stores small text files used by the lightweight RAG layer.

Planned content:

- engagement rule examples
- authorization chain examples
- planning constraint examples
- explanation templates for algorithm outputs

The first implementation should keep this local and deterministic. A vector database can be added later if needed.

