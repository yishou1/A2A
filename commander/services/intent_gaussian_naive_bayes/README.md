# Intent Gaussian Naive Bayes Service

Run from the repository root:

```powershell
python services/intent_gaussian_naive_bayes/app/main.py
```

The default port is `9033`. Readiness requires a persisted model whose SHA256
matches its training metadata.
